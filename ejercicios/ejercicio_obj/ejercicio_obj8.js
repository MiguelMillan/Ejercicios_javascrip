/* Mantener un array donde tengamos una lista de especies de animales
 y el número de patas que tiene cada uno. 
 Crear una función que calcule un número aleatorio entre y 100 para cada tipo de animal,
 de tal modo que devuelva finalmente el número de patas que hay en la granja */

 const animal = [cerdo, gallina, caballo, cabra];
 const miarray2=[4,2,4,4];
 const aleatorio= math.floor(Math.random()*100)+1;
 let item=buscar(miarray1,miarray2);

  miarray2=new patas;
  miarray1=new animal;
  let suma=0;

 function Calcularpatas(patas){
    this.buscar = function (aleatorio) {
        for ( item of this.item) {
          if (item.miarray1===miarray2);
          suma+=patas;
          return patas;
        }
      }
}    function Granja (miarray1){
        this.miarray1=miarray1;
        this.miarray2=miarray2;
        this.add=animal;
        this.animal.push(item);
    }
    console.log(suma);
