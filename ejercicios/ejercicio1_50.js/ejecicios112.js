//En una tienda efectúan un descuento a los clientes dependiendo de la cantidad de la compra. El 
//descuento se basa en

const prompt = require("prompt-sync")();
const compra = Number(prompt("compra:"));
let descuento = 0

if (compra<500); 
console.log ("entonces no recibe descuento");

let descuento (500 && 1000);(1000 && 7000);(7000 && 15000);(>=15000)
switch (true) {
  case compra {
    descuento / 1.05;
    break;
  }
  case 2: {
    descuento = 1.10;
    break;
  }
  case 3: {
    descuento = 1.20;
    break;
  }
  case 4: {
    descuento = 1.25;
    break;
  }
  default: {
    console.log("precio final:   ", compra * descuento);
  }
}
